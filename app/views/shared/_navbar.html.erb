<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Todo app</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <%= link_to 'Home', root_path, class:"nav-item nav-link" %>
      <%= link_to 'Task', tasks_path, class:"nav-item nav-link" %>
      <% if !user_signed_in? %>
        <%= link_to 'Log in', new_user_session_path, class: 'nav-item btn btn-outline-primary' %>
      <% end %>
      <% if user_signed_in? %>
        <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'nav-item btn btn-outline-danger' %>
        <a class="nav-item nav-link" href="#">User: <%= current_user.email %></a>
      <% end %>
    </div>
  </div>
</nav>